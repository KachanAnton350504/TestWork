<%= form_for(@part_of_catalog) do |f| %>
  <% if @part_of_catalog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@part_of_catalog.errors.count, "error") %> prohibited this part_of_catalog from being saved:</h2>

      <ul>
      <% @part_of_catalog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
